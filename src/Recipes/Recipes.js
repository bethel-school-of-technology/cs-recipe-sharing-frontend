import React from 'react';
import Closed from './my-recipe';

const Closed = React.createClass({
    render() {
        return (
            <div className= "closed">
                
            </div>
        );
    }
});

export default Closed;

import React from 'react';
